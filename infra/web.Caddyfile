# Serves the front-end's static bundle. Any unknown route falls back to
# index.html: routing happens in the browser.
:80 {
	root * /srv
	encode zstd gzip
	try_files {path} /index.html
	file_server
}
